// routes.js

let controllers = require('./controllers')

let api = require('./controllers')

module.exports.initialize = function(app, router){
	router.get('/', controllers.home.index)
	router.get('/api/images'. .images.list)

	app.use('/', router)
}
